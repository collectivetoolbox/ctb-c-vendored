diff -uraN /home/a/ctoolbox/vendor/winit.orig/Cargo.toml /home/a/ctoolbox/vendor/winit/Cargo.toml
--- /home/a/ctoolbox/vendor/winit.orig/Cargo.toml	2026-02-06 23:31:36.156413490 -0500
+++ /home/a/ctoolbox/vendor/winit/Cargo.toml	2026-02-08 19:30:37.495966570 -0500
@@ -10,10 +10,14 @@
 readme = "README.md"
 documentation = "https://docs.rs/winit"
 categories = ["gui"]
-rust-version.workspace = true
-repository.workspace = true
-license.workspace = true
-edition.workspace = true
+workspace = "../.."
+edition = "2024"
+rust-version = "1.85"
+license = "Apache-2.0"
+#rust-version.workspace = true
+#repository.workspace = true
+#license.workspace = true
+#edition.workspace = true
 include = [
     "/build.rs",
     "/docs",
@@ -57,8 +61,9 @@
 
 # Features are documented in either `lib.rs` or under `winit::platform`.
 [features]
-default = ["rwh_06", "x11", "wayland", "wayland-dlopen", "wayland-csd-adwaita"]
-x11 = ["x11-dl", "bytemuck", "percent-encoding", "xkbcommon-dl/x11", "x11rb"]
+# I can't figure out how to make the `wayland-csd-adwaita` feature turn off so I just deleted it.
+default = ["rwh_06", "x11", "wayland"]
+x11 = ["dep:x11", "bytemuck", "percent-encoding", "x11rb"]
 wayland = [
     "wayland-client",
     "wayland-backend",
@@ -68,10 +73,9 @@
     "ahash",
     "memmap2",
 ]
-wayland-dlopen = ["wayland-backend/dlopen"]
-wayland-csd-adwaita = ["sctk-adwaita", "sctk-adwaita/ab_glyph"]
-wayland-csd-adwaita-crossfont = ["sctk-adwaita", "sctk-adwaita/crossfont"]
-wayland-csd-adwaita-notitle = ["sctk-adwaita"]
+#wayland-csd-adwaita = ["sctk-adwaita", "sctk-adwaita/ab_glyph"]
+#wayland-csd-adwaita-crossfont = ["sctk-adwaita", "sctk-adwaita/crossfont"]
+#wayland-csd-adwaita-notitle = ["sctk-adwaita"]
 android-native-activity = ["android-activity/native-activity"]
 android-game-activity = ["android-activity/game-activity"]
 serde = ["dep:serde", "cursor-icon/serde", "smol_str/serde", "dpi/serde"]
@@ -86,7 +90,7 @@
 [dependencies]
 bitflags = "2"
 cursor-icon = "1.1.0"
-dpi = { version = "0.1.1", path = "dpi" }
+dpi = { version = "0.1.1" }
 rwh_04 = { package = "raw-window-handle", version = "0.4", optional = true }
 rwh_05 = { package = "raw-window-handle", version = "0.5.2", features = [
     "std",
@@ -94,7 +98,7 @@
 rwh_06 = { package = "raw-window-handle", version = "0.6", features = [
     "std",
 ], optional = true }
-serde = { workspace = true, optional = true }
+serde = { version = "1.0.228", optional = true }
 smol_str = "0.2.0"
 tracing = { version = "0.1.40", default-features = false }
 
@@ -107,9 +111,7 @@
 [target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dev-dependencies]
 softbuffer = { version = "0.4.0", default-features = false, features = [
     "x11",
-    "x11-dlopen",
     "wayland",
-    "wayland-dlopen",
 ] }
 
 [target.'cfg(target_os = "android")'.dependencies]
@@ -268,9 +270,7 @@
     "calloop",
 ], optional = true }
 sctk-adwaita = { version = "0.10.1", default-features = false, optional = true }
-wayland-backend = { version = "0.3.10", default-features = false, features = [
-    "client_system",
-], optional = true }
+wayland-backend = { version = "0.3.10", default-features = false, optional = true }
 wayland-client = { version = "0.31.10", optional = true }
 wayland-protocols = { version = "0.32.8", features = [
     "staging",
@@ -278,16 +278,21 @@
 wayland-protocols-plasma = { version = "0.3.8", features = [
     "client",
 ], optional = true }
-x11-dl = { version = "2.19.1", optional = true }
+x11 = { version = "2.21.0", optional = true, default-features = false, features = [
+    "xlib",
+    "xlib_xcb",
+    "xcursor",
+    "xinput",
+    "xfixes",
+] }
 x11rb = { version = "0.13.0", default-features = false, features = [
     "allow-unsafe-code",
-    "dl-libxcb",
     "randr",
     "resource_manager",
     "xinput",
     "xkb",
 ], optional = true }
-xkbcommon-dl = "0.4.2"
+xkeysym = "0.2.1"
 
 [target.'cfg(target_os = "redox")'.dependencies]
 orbclient = { version = "0.3.47", default-features = false }
@@ -362,17 +367,3 @@
 [[example]]
 doc-scrape-examples = true
 name = "window"
-
-[workspace]
-resolver = "2"
-members = ["dpi"]
-
-[workspace.package]
-rust-version = "1.70.0"
-repository = "https://github.com/rust-windowing/winit"
-license = "Apache-2.0"
-edition = "2021"
-
-[workspace.dependencies]
-serde = { version = "1", features = ["serde_derive"] }
-mint = "0.5.6"
diff -uraN /home/a/ctoolbox/vendor/winit.orig/README.md /home/a/ctoolbox/vendor/winit/README.md
--- /home/a/ctoolbox/vendor/winit.orig/README.md	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/README.md	2026-01-25 13:58:53.752058589 -0500
@@ -1,4 +1,5 @@
 # winit - Cross-platform window creation and management in Rust
+# Vendored for ctoolbox to allow static linking
 
 [![Crates.io](https://img.shields.io/crates/v/winit.svg)](https://crates.io/crates/winit)
 [![Docs.rs](https://docs.rs/winit/badge.svg)](https://docs.rs/winit)
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/keyboard.rs /home/a/ctoolbox/vendor/winit/src/keyboard.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/keyboard.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/keyboard.rs	2026-02-01 02:38:16.816436038 -0500
@@ -251,7 +251,7 @@
     #[inline]
     fn eq(&self, rhs: &KeyCode) -> bool {
         match self {
-            PhysicalKey::Code(ref code) => code == rhs,
+            PhysicalKey::Code(code) => code == rhs,
             _ => false,
         }
     }
@@ -268,7 +268,7 @@
     #[inline]
     fn eq(&self, rhs: &NativeKeyCode) -> bool {
         match self {
-            PhysicalKey::Unidentified(ref code) => code == rhs,
+            PhysicalKey::Unidentified(code) => code == rhs,
             _ => false,
         }
     }
@@ -1509,7 +1509,7 @@
     #[inline]
     fn eq(&self, rhs: &NamedKey) -> bool {
         match self {
-            Key::Named(ref a) => a == rhs,
+            Key::Named(a) => a == rhs,
             _ => false,
         }
     }
@@ -1519,7 +1519,7 @@
     #[inline]
     fn eq(&self, rhs: &str) -> bool {
         match self {
-            Key::Character(ref s) => s == rhs,
+            Key::Character(s) => s == rhs,
             _ => false,
         }
     }
@@ -1536,7 +1536,7 @@
     #[inline]
     fn eq(&self, rhs: &NativeKey) -> bool {
         match self {
-            Key::Unidentified(ref code) => code == rhs,
+            Key::Unidentified(code) => code == rhs,
             _ => false,
         }
     }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform/startup_notify.rs /home/a/ctoolbox/vendor/winit/src/platform/startup_notify.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform/startup_notify.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform/startup_notify.rs	2026-02-01 02:38:16.816436038 -0500
@@ -86,14 +86,18 @@
 /// This is wise to do before running child processes,
 /// which may not to support the activation token.
 pub fn reset_activation_token_env() {
-    env::remove_var(X11_VAR);
-    env::remove_var(WAYLAND_VAR);
+    unsafe {
+        env::remove_var(X11_VAR);
+        env::remove_var(WAYLAND_VAR);
+    }
 }
 
 /// Set environment variables responsible for activation token.
 ///
 /// This could be used before running daemon processes.
 pub fn set_activation_token_env(token: ActivationToken) {
-    env::set_var(X11_VAR, &token.token);
-    env::set_var(WAYLAND_VAR, token.token);
+    unsafe {
+        env::set_var(X11_VAR, &token.token);
+        env::set_var(WAYLAND_VAR, token.token);
+    }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform/wayland.rs /home/a/ctoolbox/vendor/winit/src/platform/wayland.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform/wayland.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform/wayland.rs	2026-02-01 02:38:16.816436038 -0500
@@ -2,8 +2,8 @@
 //!
 //! **Note:** Windows don't appear on Wayland until you draw/present to them.
 //!
-//! By default, Winit loads system libraries using `dlopen`. This can be
-//! disabled by disabling the `"wayland-dlopen"` cargo feature.
+//! This vendored build of Winit does not use `dlopen` to load Wayland
+//! libraries.
 //!
 //! ## Client-side decorations
 //!
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/compose.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/compose.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/compose.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/compose.rs	2026-02-01 02:38:16.820436103 -0500
@@ -6,9 +6,9 @@
 use std::os::unix::ffi::OsStringExt;
 use std::ptr::NonNull;
 
-use super::{XkbContext, XKBCH};
+use super::{ffi as xkb, XkbContext};
 use smol_str::SmolStr;
-use xkbcommon_dl::{
+use xkb::{
     xkb_compose_compile_flags, xkb_compose_feed_result, xkb_compose_state, xkb_compose_state_flags,
     xkb_compose_status, xkb_compose_table, xkb_keysym_t,
 };
@@ -30,7 +30,7 @@
         let locale = CString::new(locale.into_vec()).unwrap();
 
         let table = unsafe {
-            (XKBCH.xkb_compose_table_new_from_locale)(
+            xkb::xkb_compose_table_new_from_locale(
                 context.as_ptr(),
                 locale.as_ptr(),
                 xkb_compose_compile_flags::XKB_COMPOSE_COMPILE_NO_FLAGS,
@@ -44,7 +44,7 @@
     /// Create new state with the given compose table.
     pub fn new_state(&self) -> Option<XkbComposeState> {
         let state = unsafe {
-            (XKBCH.xkb_compose_state_new)(
+            xkb::xkb_compose_state_new(
                 self.table.as_ptr(),
                 xkb_compose_state_flags::XKB_COMPOSE_STATE_NO_FLAGS,
             )
@@ -66,7 +66,7 @@
 impl Drop for XkbComposeTable {
     fn drop(&mut self) {
         unsafe {
-            (XKBCH.xkb_compose_table_unref)(self.table.as_ptr());
+            xkb::xkb_compose_table_unref(self.table.as_ptr());
         }
     }
 }
@@ -79,13 +79,13 @@
 impl XkbComposeState {
     pub fn get_string(&mut self, scratch_buffer: &mut Vec<u8>) -> Option<SmolStr> {
         super::make_string_with(scratch_buffer, |ptr, len| unsafe {
-            (XKBCH.xkb_compose_state_get_utf8)(self.state.as_ptr(), ptr, len)
+            xkb::xkb_compose_state_get_utf8(self.state.as_ptr(), ptr, len)
         })
     }
 
     #[inline]
     pub fn feed(&mut self, keysym: xkb_keysym_t) -> ComposeStatus {
-        let feed_result = unsafe { (XKBCH.xkb_compose_state_feed)(self.state.as_ptr(), keysym) };
+        let feed_result = unsafe { xkb::xkb_compose_state_feed(self.state.as_ptr(), keysym) };
         match feed_result {
             xkb_compose_feed_result::XKB_COMPOSE_FEED_IGNORED => ComposeStatus::Ignored,
             xkb_compose_feed_result::XKB_COMPOSE_FEED_ACCEPTED => {
@@ -97,20 +97,20 @@
     #[inline]
     pub fn reset(&mut self) {
         unsafe {
-            (XKBCH.xkb_compose_state_reset)(self.state.as_ptr());
+            xkb::xkb_compose_state_reset(self.state.as_ptr());
         }
     }
 
     #[inline]
     pub fn status(&mut self) -> xkb_compose_status {
-        unsafe { (XKBCH.xkb_compose_state_get_status)(self.state.as_ptr()) }
+        unsafe { xkb::xkb_compose_state_get_status(self.state.as_ptr()) }
     }
 }
 
 impl Drop for XkbComposeState {
     fn drop(&mut self) {
         unsafe {
-            (XKBCH.xkb_compose_state_unref)(self.state.as_ptr());
+            xkb::xkb_compose_state_unref(self.state.as_ptr());
         };
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/ffi.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/ffi.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/ffi.rs	1969-12-31 19:00:00.000000000 -0500
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/ffi.rs	2026-02-01 02:38:16.820436103 -0500
@@ -0,0 +1,213 @@
+#![allow(dead_code, non_camel_case_types)]
+
+use std::os::raw::{c_char, c_int, c_uint};
+
+pub const XKB_MOD_NAME_SHIFT: &[u8] = b"Shift\0";
+pub const XKB_MOD_NAME_CAPS: &[u8] = b"Lock\0";
+pub const XKB_MOD_NAME_CTRL: &[u8] = b"Control\0";
+pub const XKB_MOD_NAME_ALT: &[u8] = b"Mod1\0";
+pub const XKB_MOD_NAME_NUM: &[u8] = b"Mod2\0";
+pub const XKB_MOD_NAME_LOGO: &[u8] = b"Mod4\0";
+
+pub const XKB_MOD_INVALID: u32 = 0xffffffff;
+
+pub type xkb_keycode_t = u32;
+pub type xkb_keysym_t = u32;
+pub type xkb_layout_index_t = u32;
+pub type xkb_layout_mask_t = u32;
+pub type xkb_mod_index_t = u32;
+pub type xkb_mod_mask_t = u32;
+
+#[repr(C)]
+pub struct xkb_context {
+    _private: [u8; 0],
+}
+
+#[repr(C)]
+pub struct xkb_keymap {
+    _private: [u8; 0],
+}
+
+#[repr(C)]
+pub struct xkb_state {
+    _private: [u8; 0],
+}
+
+#[repr(C)]
+pub struct xkb_compose_table {
+    _private: [u8; 0],
+}
+
+#[repr(C)]
+pub struct xkb_compose_state {
+    _private: [u8; 0],
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_context_flags {
+    XKB_CONTEXT_NO_FLAGS = 0,
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_keymap_compile_flags {
+    XKB_KEYMAP_COMPILE_NO_FLAGS = 0,
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_keymap_format {
+    XKB_KEYMAP_FORMAT_TEXT_V1 = 1,
+}
+
+// NOTE: In libxkbcommon, `xkb_state_component` is a C `enum` type.
+//
+// We intentionally represent it as an integer type here (rather than a Rust
+// `struct`/bitflags wrapper) to ensure the extern "C" ABI matches on all
+// platforms (notably i386).
+pub type xkb_state_component = c_uint;
+
+pub const XKB_STATE_MODS_DEPRESSED: xkb_state_component = 1 << 0;
+pub const XKB_STATE_MODS_LATCHED: xkb_state_component = 1 << 1;
+pub const XKB_STATE_MODS_LOCKED: xkb_state_component = 1 << 2;
+pub const XKB_STATE_MODS_EFFECTIVE: xkb_state_component = 1 << 3;
+pub const XKB_STATE_LAYOUT_DEPRESSED: xkb_state_component = 1 << 4;
+pub const XKB_STATE_LAYOUT_LATCHED: xkb_state_component = 1 << 5;
+pub const XKB_STATE_LAYOUT_LOCKED: xkb_state_component = 1 << 6;
+pub const XKB_STATE_LAYOUT_EFFECTIVE: xkb_state_component = 1 << 7;
+pub const XKB_STATE_LEDS: xkb_state_component = 1 << 8;
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_compose_compile_flags {
+    XKB_COMPOSE_COMPILE_NO_FLAGS = 0,
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_compose_state_flags {
+    XKB_COMPOSE_STATE_NO_FLAGS = 0,
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_compose_status {
+    XKB_COMPOSE_NOTHING = 0,
+    XKB_COMPOSE_COMPOSING = 1,
+    XKB_COMPOSE_COMPOSED = 2,
+    XKB_COMPOSE_CANCELLED = 3,
+}
+
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_compose_feed_result {
+    XKB_COMPOSE_FEED_IGNORED = 0,
+    XKB_COMPOSE_FEED_ACCEPTED = 1,
+}
+
+#[link(name = "xkbcommon")]
+unsafe extern "C" {
+    pub fn xkb_keysym_to_utf8(keysym: xkb_keysym_t, buffer: *mut c_char, size: usize) -> c_int;
+
+    pub fn xkb_context_new(flags: xkb_context_flags) -> *mut xkb_context;
+    pub fn xkb_context_unref(context: *mut xkb_context);
+
+    pub fn xkb_keymap_new_from_string(
+        context: *mut xkb_context,
+        string: *const c_char,
+        format: xkb_keymap_format,
+        flags: xkb_keymap_compile_flags,
+    ) -> *mut xkb_keymap;
+    pub fn xkb_keymap_key_get_syms_by_level(
+        keymap: *mut xkb_keymap,
+        key: xkb_keycode_t,
+        layout: xkb_layout_index_t,
+        level: u32,
+        syms_out: *mut *const xkb_keysym_t,
+    ) -> c_int;
+    pub fn xkb_keymap_key_repeats(keymap: *mut xkb_keymap, key: xkb_keycode_t) -> c_int;
+    pub fn xkb_keymap_mod_get_index(keymap: *mut xkb_keymap, name: *const c_char) -> xkb_mod_index_t;
+    pub fn xkb_keymap_unref(keymap: *mut xkb_keymap);
+
+    pub fn xkb_state_new(keymap: *mut xkb_keymap) -> *mut xkb_state;
+    pub fn xkb_state_key_get_one_sym(state: *mut xkb_state, key: xkb_keycode_t) -> xkb_keysym_t;
+    pub fn xkb_state_key_get_layout(state: *mut xkb_state, key: xkb_keycode_t) -> xkb_layout_index_t;
+    pub fn xkb_state_serialize_mods(state: *mut xkb_state, components: xkb_state_component) -> xkb_mod_mask_t;
+    pub fn xkb_state_key_get_utf8(state: *mut xkb_state, key: xkb_keycode_t, buffer: *mut c_char, size: usize) -> c_int;
+    pub fn xkb_state_update_mask(
+        state: *mut xkb_state,
+        depressed_mods: xkb_mod_mask_t,
+        latched_mods: xkb_mod_mask_t,
+        locked_mods: xkb_mod_mask_t,
+        depressed_layout: xkb_layout_mask_t,
+        latched_layout: xkb_layout_mask_t,
+        locked_layout: xkb_layout_mask_t,
+    ) -> xkb_state_component;
+    pub fn xkb_state_mod_name_is_active(
+        state: *mut xkb_state,
+        name: *const c_char,
+        component: xkb_state_component,
+    ) -> c_int;
+    pub fn xkb_state_unref(state: *mut xkb_state);
+
+    pub fn xkb_compose_table_new_from_locale(
+        context: *mut xkb_context,
+        locale: *const c_char,
+        flags: xkb_compose_compile_flags,
+    ) -> *mut xkb_compose_table;
+    pub fn xkb_compose_table_unref(table: *mut xkb_compose_table);
+
+    pub fn xkb_compose_state_new(
+        table: *mut xkb_compose_table,
+        flags: xkb_compose_state_flags,
+    ) -> *mut xkb_compose_state;
+    pub fn xkb_compose_state_unref(state: *mut xkb_compose_state);
+    pub fn xkb_compose_state_get_utf8(
+        state: *mut xkb_compose_state,
+        buffer: *mut c_char,
+        size: usize,
+    ) -> c_int;
+    pub fn xkb_compose_state_feed(state: *mut xkb_compose_state, keysym: xkb_keysym_t) -> xkb_compose_feed_result;
+    pub fn xkb_compose_state_reset(state: *mut xkb_compose_state);
+    pub fn xkb_compose_state_get_status(state: *mut xkb_compose_state) -> xkb_compose_status;
+}
+
+#[cfg(x11_platform)]
+#[repr(C)]
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum xkb_x11_setup_xkb_extension_flags {
+    XKB_X11_SETUP_XKB_EXTENSION_NO_FLAGS = 0,
+}
+
+#[cfg(x11_platform)]
+#[link(name = "xkbcommon-x11")]
+unsafe extern "C" {
+    pub fn xkb_x11_setup_xkb_extension(
+        conn: *mut crate::platform_impl::linux::x11::ffi::xcb_connection_t,
+        major_xkb_version: c_uint,
+        minor_xkb_version: c_uint,
+        flags: xkb_x11_setup_xkb_extension_flags,
+        major_rtrn: *mut c_uint,
+        minor_rtrn: *mut c_uint,
+        base_event_out: *mut c_int,
+        base_error_out: *mut c_int,
+    ) -> c_int;
+
+    pub fn xkb_x11_get_core_keyboard_device_id(
+        conn: *mut crate::platform_impl::linux::x11::ffi::xcb_connection_t,
+    ) -> c_int;
+
+    pub fn xkb_x11_keymap_new_from_device(
+        context: *mut xkb_context,
+        conn: *mut crate::platform_impl::linux::x11::ffi::xcb_connection_t,
+        device_id: c_int,
+        flags: xkb_keymap_compile_flags,
+    ) -> *mut xkb_keymap;
+
+    pub fn xkb_x11_state_new_from_device(
+        keymap: *mut xkb_keymap,
+        conn: *mut crate::platform_impl::linux::x11::ffi::xcb_connection_t,
+        device_id: c_int,
+    ) -> *mut xkb_state;
+}
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/keymap.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/keymap.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/keymap.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/keymap.rs	2026-02-01 02:38:16.820436103 -0500
@@ -5,20 +5,20 @@
 use std::ptr::{self, NonNull};
 
 #[cfg(x11_platform)]
-use x11_dl::xlib_xcb::xcb_connection_t;
+use crate::platform_impl::linux::x11::ffi::xcb_connection_t;
 #[cfg(wayland_platform)]
 use {memmap2::MmapOptions, std::os::unix::io::OwnedFd};
 
-use xkb::XKB_MOD_INVALID;
-use xkbcommon_dl::{
-    self as xkb, xkb_keycode_t, xkb_keymap, xkb_keymap_compile_flags, xkb_keysym_t,
-    xkb_layout_index_t, xkb_mod_index_t,
+use super::ffi as xkb;
+use super::ffi::{
+    xkb_keycode_t, xkb_keymap, xkb_keymap_compile_flags, xkb_keysym_t, xkb_layout_index_t,
+    xkb_mod_index_t,
 };
 
+use xkb::XKB_MOD_INVALID;
+
 use crate::keyboard::{Key, KeyCode, KeyLocation, NamedKey, NativeKey, NativeKeyCode, PhysicalKey};
-#[cfg(x11_platform)]
-use crate::platform_impl::common::xkb::XKBXH;
-use crate::platform_impl::common::xkb::{XkbContext, XKBH};
+use crate::platform_impl::common::xkb::XkbContext;
 
 /// Map the raw X11-style keycode to the `KeyCode` enum.
 ///
@@ -441,7 +441,7 @@
 }
 
 pub fn keysym_to_key(keysym: u32) -> Key {
-    use xkbcommon_dl::keysyms;
+    use xkeysym::key as keysyms;
     Key::Named(match keysym {
         // TTY function keys
         keysyms::BackSpace => NamedKey::Backspace,
@@ -855,7 +855,7 @@
 }
 
 pub fn keysym_location(keysym: u32) -> KeyLocation {
-    use xkbcommon_dl::keysyms;
+    use xkeysym::key as keysyms;
     match keysym {
         keysyms::Shift_L
         | keysyms::Control_L
@@ -920,10 +920,20 @@
     pub fn from_fd(context: &XkbContext, fd: OwnedFd, size: usize) -> Option<Self> {
         let map = unsafe { MmapOptions::new().len(size).map_copy_read_only(&fd).ok()? };
 
+        // `xkb_keymap_new_from_string` expects a NUL-terminated C string. While
+        // the Wayland protocol specifies that the keymap is NUL-terminated,
+        // it's safer to enforce this here to avoid reading past the mapped
+        // region on buggy compositors or size mismatches.
+        let mut map_buf = Vec::with_capacity(map.len().saturating_add(1));
+        map_buf.extend_from_slice(&map);
+        if !map_buf.ends_with(&[0]) {
+            map_buf.push(0);
+        }
+
         let keymap = unsafe {
-            let keymap = (XKBH.xkb_keymap_new_from_string)(
+            let keymap = xkb::xkb_keymap_new_from_string(
                 (*context).as_ptr(),
-                map.as_ptr() as *const _,
+                map_buf.as_ptr() as *const _,
                 xkb::xkb_keymap_format::XKB_KEYMAP_FORMAT_TEXT_V1,
                 xkb_keymap_compile_flags::XKB_KEYMAP_COMPILE_NO_FLAGS,
             );
@@ -940,7 +950,7 @@
         core_keyboard_id: i32,
     ) -> Option<Self> {
         let keymap = unsafe {
-            (XKBXH.xkb_x11_keymap_new_from_device)(
+            xkb::xkb_x11_keymap_new_from_device(
                 context.as_ptr(),
                 xcb,
                 core_keyboard_id,
@@ -978,7 +988,7 @@
     ) -> xkb_keysym_t {
         unsafe {
             let mut keysyms = ptr::null();
-            let count = (XKBH.xkb_keymap_key_get_syms_by_level)(
+            let count = xkb::xkb_keymap_key_get_syms_by_level(
                 self.keymap.as_ptr(),
                 keycode,
                 layout,
@@ -997,14 +1007,14 @@
 
     /// Check whether the given key repeats.
     pub fn key_repeats(&mut self, keycode: xkb_keycode_t) -> bool {
-        unsafe { (XKBH.xkb_keymap_key_repeats)(self.keymap.as_ptr(), keycode) == 1 }
+        unsafe { xkb::xkb_keymap_key_repeats(self.keymap.as_ptr(), keycode) == 1 }
     }
 }
 
 impl Drop for XkbKeymap {
     fn drop(&mut self) {
         unsafe {
-            (XKBH.xkb_keymap_unref)(self.keymap.as_ptr());
+            xkb::xkb_keymap_unref(self.keymap.as_ptr());
         };
     }
 }
@@ -1034,7 +1044,7 @@
 fn mod_index_for_name(keymap: NonNull<xkb_keymap>, name: &[u8]) -> Option<xkb_mod_index_t> {
     unsafe {
         let mod_index =
-            (XKBH.xkb_keymap_mod_get_index)(keymap.as_ptr(), name.as_ptr() as *const c_char);
+            xkb::xkb_keymap_mod_get_index(keymap.as_ptr(), name.as_ptr() as *const c_char);
         if mod_index == XKB_MOD_INVALID {
             None
         } else {
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -3,26 +3,27 @@
 use std::ptr::{self, NonNull};
 use std::sync::atomic::{AtomicBool, Ordering};
 
-use crate::utils::Lazy;
 use smol_str::SmolStr;
 #[cfg(wayland_platform)]
 use std::os::unix::io::OwnedFd;
 use tracing::warn;
-use xkbcommon_dl::{
-    self as xkb, xkb_compose_status, xkb_context, xkb_context_flags, xkbcommon_compose_handle,
-    xkbcommon_handle, XkbCommon, XkbCommonCompose,
-};
+
+use self::ffi::{xkb_compose_status, xkb_context, xkb_context_flags};
+
 #[cfg(x11_platform)]
-use {x11_dl::xlib_xcb::xcb_connection_t, xkbcommon_dl::x11::xkbcommon_x11_handle};
+use crate::platform_impl::linux::x11::ffi::xcb_connection_t;
 
 use crate::event::{ElementState, KeyEvent};
 use crate::keyboard::{Key, KeyLocation};
 use crate::platform_impl::KeyEventExtra;
 
 mod compose;
+mod ffi;
 mod keymap;
 mod state;
 
+pub(crate) use self::ffi::xkb_mod_mask_t;
+
 use compose::{ComposeStatus, XkbComposeState, XkbComposeTable};
 use keymap::XkbKeymap;
 
@@ -34,11 +35,6 @@
 // TODO: Wire this up without using a static `AtomicBool`.
 static RESET_DEAD_KEYS: AtomicBool = AtomicBool::new(false);
 
-static XKBH: Lazy<&'static XkbCommon> = Lazy::new(xkbcommon_handle);
-static XKBCH: Lazy<&'static XkbCommonCompose> = Lazy::new(xkbcommon_compose_handle);
-#[cfg(feature = "x11")]
-static XKBXH: Lazy<&'static xkb::x11::XkbCommonX11> = Lazy::new(xkbcommon_x11_handle);
-
 #[inline(always)]
 pub fn reset_dead_keys() {
     RESET_DEAD_KEYS.store(true, Ordering::SeqCst);
@@ -66,10 +62,6 @@
 
 impl Context {
     pub fn new() -> Result<Self, Error> {
-        if xkb::xkbcommon_option().is_none() {
-            return Err(Error::XKBNotFound);
-        }
-
         let context = XkbContext::new()?;
         let mut compose_table = XkbComposeTable::new(&context);
         let mut compose_state1 = compose_table.as_ref().and_then(|table| table.new_state());
@@ -95,14 +87,14 @@
         })
     }
 
-    #[cfg(feature = "x11")]
+    #[cfg(x11_platform)]
     pub fn from_x11_xkb(xcb: *mut xcb_connection_t) -> Result<Self, Error> {
         let result = unsafe {
-            (XKBXH.xkb_x11_setup_xkb_extension)(
+            ffi::xkb_x11_setup_xkb_extension(
                 xcb,
                 1,
                 2,
-                xkbcommon_dl::x11::xkb_x11_setup_xkb_extension_flags::XKB_X11_SETUP_XKB_EXTENSION_NO_FLAGS,
+                ffi::xkb_x11_setup_xkb_extension_flags::XKB_X11_SETUP_XKB_EXTENSION_NO_FLAGS,
                 ptr::null_mut(),
                 ptr::null_mut(),
                 ptr::null_mut(),
@@ -115,7 +107,7 @@
         }
 
         let mut this = Self::new()?;
-        this.core_keyboard_id = unsafe { (XKBXH.xkb_x11_get_core_keyboard_device_id)(xcb) };
+        this.core_keyboard_id = unsafe { ffi::xkb_x11_get_core_keyboard_device_id(xcb) };
         this.set_keymap_from_x11(xcb);
         Ok(this)
     }
@@ -209,7 +201,7 @@
         self.scratch_buffer.reserve(8);
         loop {
             let bytes_written = unsafe {
-                (XKBH.xkb_keysym_to_utf8)(
+                ffi::xkb_keysym_to_utf8(
                     keysym,
                     self.scratch_buffer.as_mut_ptr().cast(),
                     self.scratch_buffer.capacity(),
@@ -351,7 +343,7 @@
 
 impl XkbContext {
     pub fn new() -> Result<Self, Error> {
-        let context = unsafe { (XKBH.xkb_context_new)(xkb_context_flags::XKB_CONTEXT_NO_FLAGS) };
+        let context = unsafe { ffi::xkb_context_new(xkb_context_flags::XKB_CONTEXT_NO_FLAGS) };
 
         let context = match NonNull::new(context) {
             Some(context) => context,
@@ -365,7 +357,7 @@
 impl Drop for XkbContext {
     fn drop(&mut self) {
         unsafe {
-            (XKBH.xkb_context_unref)(self.context.as_ptr());
+            ffi::xkb_context_unref(self.context.as_ptr());
         }
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/state.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/state.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/common/xkb/state.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/common/xkb/state.rs	2026-02-01 02:38:16.820436103 -0500
@@ -4,16 +4,16 @@
 use std::ptr::NonNull;
 
 use smol_str::SmolStr;
+
 #[cfg(x11_platform)]
-use x11_dl::xlib_xcb::xcb_connection_t;
-use xkbcommon_dl::{
-    self as xkb, xkb_keycode_t, xkb_keysym_t, xkb_layout_index_t, xkb_state, xkb_state_component,
-};
+use super::ffi as xkb;
+use super::ffi::{xkb_keycode_t, xkb_keysym_t, xkb_layout_index_t, xkb_state};
 
-use crate::platform_impl::common::xkb::keymap::XkbKeymap;
 #[cfg(x11_platform)]
-use crate::platform_impl::common::xkb::XKBXH;
-use crate::platform_impl::common::xkb::{make_string_with, XKBH};
+use crate::platform_impl::linux::x11::ffi::xcb_connection_t;
+
+use crate::platform_impl::common::xkb::keymap::XkbKeymap;
+use crate::platform_impl::common::xkb::make_string_with;
 
 #[derive(Debug)]
 pub struct XkbState {
@@ -24,14 +24,14 @@
 impl XkbState {
     #[cfg(wayland_platform)]
     pub fn new_wayland(keymap: &XkbKeymap) -> Option<Self> {
-        let state = NonNull::new(unsafe { (XKBH.xkb_state_new)(keymap.as_ptr()) })?;
+        let state = NonNull::new(unsafe { xkb::xkb_state_new(keymap.as_ptr()) })?;
         Some(Self::new_inner(state))
     }
 
     #[cfg(x11_platform)]
     pub fn new_x11(xcb: *mut xcb_connection_t, keymap: &XkbKeymap) -> Option<Self> {
         let state = unsafe {
-            (XKBXH.xkb_x11_state_new_from_device)(keymap.as_ptr(), xcb, keymap._core_keyboard_id)
+            xkb::xkb_x11_state_new_from_device(keymap.as_ptr(), xcb, keymap._core_keyboard_id)
         };
         let state = NonNull::new(state)?;
         Some(Self::new_inner(state))
@@ -45,19 +45,19 @@
     }
 
     pub fn get_one_sym_raw(&mut self, keycode: xkb_keycode_t) -> xkb_keysym_t {
-        unsafe { (XKBH.xkb_state_key_get_one_sym)(self.state.as_ptr(), keycode) }
+        unsafe { xkb::xkb_state_key_get_one_sym(self.state.as_ptr(), keycode) }
     }
 
     pub fn layout(&mut self, key: xkb_keycode_t) -> xkb_layout_index_t {
-        unsafe { (XKBH.xkb_state_key_get_layout)(self.state.as_ptr(), key) }
+        unsafe { xkb::xkb_state_key_get_layout(self.state.as_ptr(), key) }
     }
 
     #[cfg(x11_platform)]
     pub fn depressed_modifiers(&mut self) -> xkb::xkb_mod_mask_t {
         unsafe {
-            (XKBH.xkb_state_serialize_mods)(
+            xkb::xkb_state_serialize_mods(
                 self.state.as_ptr(),
-                xkb_state_component::XKB_STATE_MODS_DEPRESSED,
+                xkb::XKB_STATE_MODS_DEPRESSED,
             )
         }
     }
@@ -65,9 +65,9 @@
     #[cfg(x11_platform)]
     pub fn latched_modifiers(&mut self) -> xkb::xkb_mod_mask_t {
         unsafe {
-            (XKBH.xkb_state_serialize_mods)(
+            xkb::xkb_state_serialize_mods(
                 self.state.as_ptr(),
-                xkb_state_component::XKB_STATE_MODS_LATCHED,
+                xkb::XKB_STATE_MODS_LATCHED,
             )
         }
     }
@@ -75,9 +75,9 @@
     #[cfg(x11_platform)]
     pub fn locked_modifiers(&mut self) -> xkb::xkb_mod_mask_t {
         unsafe {
-            (XKBH.xkb_state_serialize_mods)(
+            xkb::xkb_state_serialize_mods(
                 self.state.as_ptr(),
-                xkb_state_component::XKB_STATE_MODS_LOCKED,
+                xkb::XKB_STATE_MODS_LOCKED,
             )
         }
     }
@@ -88,7 +88,7 @@
         scratch_buffer: &mut Vec<u8>,
     ) -> Option<SmolStr> {
         make_string_with(scratch_buffer, |ptr, len| unsafe {
-            (XKBH.xkb_state_key_get_utf8)(self.state.as_ptr(), keycode, ptr, len)
+            xkb::xkb_state_key_get_utf8(self.state.as_ptr(), keycode, ptr, len)
         })
     }
 
@@ -106,7 +106,7 @@
         locked_group: u32,
     ) {
         let mask = unsafe {
-            (XKBH.xkb_state_update_mask)(
+            xkb::xkb_state_update_mask(
                 self.state.as_ptr(),
                 mods_depressed,
                 mods_latched,
@@ -117,7 +117,7 @@
             )
         };
 
-        if mask.contains(xkb_state_component::XKB_STATE_MODS_EFFECTIVE) {
+        if (mask & xkb::XKB_STATE_MODS_EFFECTIVE) != 0 {
             // Effective value of mods have changed, we need to update our state.
             self.reload_modifiers();
         }
@@ -136,10 +136,10 @@
     /// Check if the modifier is active within xkb.
     fn mod_name_is_active(&mut self, name: &[u8]) -> bool {
         unsafe {
-            (XKBH.xkb_state_mod_name_is_active)(
+            xkb::xkb_state_mod_name_is_active(
                 self.state.as_ptr(),
                 name.as_ptr() as *const c_char,
-                xkb_state_component::XKB_STATE_MODS_EFFECTIVE,
+                xkb::XKB_STATE_MODS_EFFECTIVE,
             ) > 0
         }
     }
@@ -148,7 +148,7 @@
 impl Drop for XkbState {
     fn drop(&mut self) {
         unsafe {
-            (XKBH.xkb_state_unref)(self.state.as_ptr());
+            xkb::xkb_state_unref(self.state.as_ptr());
         }
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -251,7 +251,7 @@
     }
 
     #[inline]
-    pub fn video_modes(&self) -> Box<dyn Iterator<Item = VideoModeHandle>> {
+    pub fn video_modes(&self) -> Box<dyn Iterator<Item = VideoModeHandle> + '_> {
         x11_or_wayland!(match self; MonitorHandle(m) => Box::new(m.video_modes()))
     }
 }
@@ -560,11 +560,11 @@
     pub fn available_monitors(&self) -> VecDeque<MonitorHandle> {
         match self {
             #[cfg(x11_platform)]
-            Window::X(ref window) => {
+            Window::X(window) => {
                 window.available_monitors().into_iter().map(MonitorHandle::X).collect()
             },
             #[cfg(wayland_platform)]
-            Window::Wayland(ref window) => {
+            Window::Wayland(window) => {
                 window.available_monitors().into_iter().map(MonitorHandle::Wayland).collect()
             },
         }
@@ -666,7 +666,9 @@
         // which do not require initialization.
         let mut buf: [MaybeUninit<c_char>; 1024] = unsafe { MaybeUninit::uninit().assume_init() };
         unsafe {
-            (xconn.xlib.XGetErrorText)(
+            use crate::platform_impl::x11::ffi;
+
+            ffi::XGetErrorText(
                 display,
                 (*event).error_code as c_int,
                 buf.as_mut_ptr() as *mut c_char,
@@ -982,10 +984,8 @@
 
             #[cfg(wayland_platform)]
             Self::Wayland(conn) => {
-                use sctk::reexports::client::Proxy;
-
                 let mut wayland_handle = rwh_05::WaylandDisplayHandle::empty();
-                wayland_handle.display = conn.display().id().as_ptr() as *mut _;
+                wayland_handle.display = std::ptr::NonNull::from(conn).cast().as_ptr();
                 wayland_handle.into()
             },
         }
@@ -1008,10 +1008,8 @@
 
             #[cfg(wayland_platform)]
             Self::Wayland(conn) => {
-                use sctk::reexports::client::Proxy;
-
                 Ok(rwh_06::WaylandDisplayHandle::new(
-                    NonNull::new(conn.display().id().as_ptr().cast()).unwrap(),
+                    NonNull::from(conn).cast(),
                 )
                 .into())
             },
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/event_loop/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/event_loop/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/event_loop/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/event_loop/mod.rs	2026-02-07 01:01:53.030589147 -0500
@@ -106,6 +106,7 @@
         let wayland_source = WaylandSource::new(connection.clone(), event_queue);
         let wayland_dispatcher =
             calloop::Dispatcher::new(wayland_source, |_, queue, winit_state: &mut WinitState| {
+                // tracing::warn!("Dispatching pending events");
                 let result = queue.dispatch_pending(winit_state);
                 if result.is_ok()
                     && (!winit_state.events_sink.is_empty()
@@ -696,10 +697,8 @@
     #[cfg(feature = "rwh_05")]
     #[inline]
     pub fn raw_display_handle_rwh_05(&self) -> rwh_05::RawDisplayHandle {
-        use sctk::reexports::client::Proxy;
-
         let mut display_handle = rwh_05::WaylandDisplayHandle::empty();
-        display_handle.display = self.connection.display().id().as_ptr() as *mut _;
+        display_handle.display = std::ptr::NonNull::from(&self.connection).cast().as_ptr();
         rwh_05::RawDisplayHandle::Wayland(display_handle)
     }
 
@@ -708,13 +707,7 @@
     pub fn raw_display_handle_rwh_06(
         &self,
     ) -> Result<rwh_06::RawDisplayHandle, rwh_06::HandleError> {
-        use sctk::reexports::client::Proxy;
-
-        Ok(rwh_06::WaylandDisplayHandle::new({
-            let ptr = self.connection.display().id().as_ptr();
-            std::ptr::NonNull::new(ptr as *mut _).expect("wl_display should never be null")
-        })
-        .into())
+        Ok(rwh_06::WaylandDisplayHandle::new(std::ptr::NonNull::from(&self.connection).cast()).into())
     }
 }
 
@@ -784,7 +777,7 @@
                         }
                         let _ = rustix::event::poll(&mut [poll_fd, pipe_poll_fd], -1);
                         // Non-blocking read the connection.
-                        let _ = read_guard.read_without_dispatch();
+                        let _ = read_guard.read();
                     }
 
                     awakener.ping();
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -5,7 +5,8 @@
 
 use sctk::reexports::client::globals::{BindError, GlobalError};
 use sctk::reexports::client::protocol::wl_surface::WlSurface;
-use sctk::reexports::client::{self, ConnectError, DispatchError, Proxy};
+use sctk::reexports::client::{self, ConnectError, DispatchError};
+use sctk::reexports::client::Proxy;
 
 pub(super) use crate::cursor::OnlyCursorImage as CustomCursor;
 use crate::dpi::{LogicalSize, PhysicalSize};
@@ -74,7 +75,12 @@
 /// Get the WindowId out of the surface.
 #[inline]
 fn make_wid(surface: &WlSurface) -> WindowId {
-    WindowId(surface.id().as_ptr() as u64)
+    // Use the Wayland object id, which is stable across `Proxy` clones.
+    //
+    // Using the Rust wrapper's address breaks because clones of a `WlSurface`
+    // are distinct wrapper values; that can cause compositor events (like
+    // `configure`) to be associated with the wrong `WindowId`.
+    WindowId(u64::from(surface.id().protocol_id()))
 }
 
 /// The default routine does floor, but we need round on Wayland.
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/seat/keyboard/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/seat/keyboard/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/seat/keyboard/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/seat/keyboard/mod.rs	2026-02-07 02:19:25.553470098 -0500
@@ -5,7 +5,6 @@
 
 use calloop::timer::{TimeoutAction, Timer};
 use calloop::{LoopHandle, RegistrationToken};
-use tracing::warn;
 
 use sctk::reexports::client::protocol::wl_keyboard::{
     Event as WlKeyboardEvent, KeyState as WlKeyState, KeymapFormat as WlKeymapFormat, WlKeyboard,
@@ -33,23 +32,22 @@
         let seat_state = match state.seats.get_mut(&data.seat.id()) {
             Some(seat_state) => seat_state,
             None => {
-                warn!("Received keyboard event {event:?} without seat");
+                tracing::warn!("Received keyboard event {event:?} without seat");
                 return;
             },
         };
         let keyboard_state = match seat_state.keyboard_state.as_mut() {
             Some(keyboard_state) => keyboard_state,
             None => {
-                warn!("Received keyboard event {event:?} without keyboard");
+                tracing::warn!("Received keyboard event {event:?} without keyboard");
                 return;
             },
         };
-
         match event {
             WlKeyboardEvent::Keymap { format, fd, size } => match format {
                 WEnum::Value(format) => match format {
                     WlKeymapFormat::NoKeymap => {
-                        warn!("non-xkb compatible keymap")
+                        tracing::warn!("non-xkb compatible keymap")
                     },
                     WlKeymapFormat::XkbV1 => {
                         let context = &mut keyboard_state.xkb_context;
@@ -58,7 +56,7 @@
                     _ => unreachable!(),
                 },
                 WEnum::Unknown(value) => {
-                    warn!("unknown keymap format 0x{:x}", value)
+                    tracing::warn!("unknown keymap format 0x{:x}", value)
                 },
             },
             WlKeyboardEvent::Enter { surface, .. } => {
@@ -133,7 +131,6 @@
             },
             WlKeyboardEvent::Key { key, state: WEnum::Value(WlKeyState::Pressed), .. } => {
                 let key = key + 8;
-
                 key_input(
                     keyboard_state,
                     &mut state.events_sink,
@@ -185,7 +182,6 @@
                             Some(repeat_keycode) => repeat_keycode,
                             None => return TimeoutAction::Drop,
                         };
-
                         key_input(
                             keyboard_state,
                             &mut state.events_sink,
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/window/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/window/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/wayland/window/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/wayland/window/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -5,9 +5,8 @@
 use std::sync::atomic::{AtomicBool, Ordering};
 use std::sync::{Arc, Mutex};
 
-use sctk::reexports::client::protocol::wl_display::WlDisplay;
 use sctk::reexports::client::protocol::wl_surface::WlSurface;
-use sctk::reexports::client::{Proxy, QueueHandle};
+use sctk::reexports::client::{Connection, Proxy, QueueHandle};
 
 use sctk::compositor::{CompositorState, Region, SurfaceData};
 use sctk::reexports::protocols::xdg::activation::v1::client::xdg_activation_v1::XdgActivationV1;
@@ -52,9 +51,9 @@
     /// Compositor to handle WlRegion stuff.
     compositor: Arc<CompositorState>,
 
-    /// The wayland display used solely for raw window handle.
+    /// The wayland connection used solely for raw display handle.
     #[allow(dead_code)]
-    display: WlDisplay,
+    connection: Connection,
 
     /// Xdg activation to request user attention.
     xdg_activation: Option<XdgActivationV1>,
@@ -92,7 +91,7 @@
         let compositor = state.compositor_state.clone();
         let xdg_activation =
             state.xdg_activation.as_ref().map(|activation_state| activation_state.global().clone());
-        let display = event_loop_window_target.connection.display();
+        let connection = event_loop_window_target.connection.clone();
 
         let size: Size = attributes.inner_size.unwrap_or(LogicalSize::new(800., 600.).into());
 
@@ -212,7 +211,7 @@
 
         Ok(Self {
             window,
-            display,
+            connection,
             monitors,
             window_id,
             compositor,
@@ -227,7 +226,7 @@
     }
 
     pub(crate) fn xdg_toplevel(&self) -> Option<NonNull<c_void>> {
-        NonNull::new(self.window.xdg_toplevel().id().as_ptr().cast())
+        Some(NonNull::from(self.window.xdg_toplevel()).cast())
     }
 }
 
@@ -641,8 +640,8 @@
     #[inline]
     pub fn raw_window_handle_rwh_04(&self) -> rwh_04::RawWindowHandle {
         let mut window_handle = rwh_04::WaylandHandle::empty();
-        window_handle.surface = self.window.wl_surface().id().as_ptr() as *mut _;
-        window_handle.display = self.display.id().as_ptr() as *mut _;
+        window_handle.surface = std::ptr::NonNull::from(self.window.wl_surface()).cast().as_ptr();
+        window_handle.display = std::ptr::NonNull::from(&self.connection).cast().as_ptr();
         rwh_04::RawWindowHandle::Wayland(window_handle)
     }
 
@@ -650,7 +649,7 @@
     #[inline]
     pub fn raw_window_handle_rwh_05(&self) -> rwh_05::RawWindowHandle {
         let mut window_handle = rwh_05::WaylandWindowHandle::empty();
-        window_handle.surface = self.window.wl_surface().id().as_ptr() as *mut _;
+        window_handle.surface = std::ptr::NonNull::from(self.window.wl_surface()).cast().as_ptr();
         rwh_05::RawWindowHandle::Wayland(window_handle)
     }
 
@@ -658,18 +657,14 @@
     #[inline]
     pub fn raw_display_handle_rwh_05(&self) -> rwh_05::RawDisplayHandle {
         let mut display_handle = rwh_05::WaylandDisplayHandle::empty();
-        display_handle.display = self.display.id().as_ptr() as *mut _;
+        display_handle.display = std::ptr::NonNull::from(&self.connection).cast().as_ptr();
         rwh_05::RawDisplayHandle::Wayland(display_handle)
     }
 
     #[cfg(feature = "rwh_06")]
     #[inline]
     pub fn raw_window_handle_rwh_06(&self) -> Result<rwh_06::RawWindowHandle, rwh_06::HandleError> {
-        Ok(rwh_06::WaylandWindowHandle::new({
-            let ptr = self.window.wl_surface().id().as_ptr();
-            std::ptr::NonNull::new(ptr as *mut _).expect("wl_surface will never be null")
-        })
-        .into())
+        Ok(rwh_06::WaylandWindowHandle::new(std::ptr::NonNull::from(self.window.wl_surface()).cast()).into())
     }
 
     #[cfg(feature = "rwh_06")]
@@ -677,11 +672,7 @@
     pub fn raw_display_handle_rwh_06(
         &self,
     ) -> Result<rwh_06::RawDisplayHandle, rwh_06::HandleError> {
-        Ok(rwh_06::WaylandDisplayHandle::new({
-            let ptr = self.display.id().as_ptr();
-            std::ptr::NonNull::new(ptr as *mut _).expect("wl_proxy should never be null")
-        })
-        .into())
+        Ok(rwh_06::WaylandDisplayHandle::new(std::ptr::NonNull::from(&self.connection).cast()).into())
     }
 
     #[inline]
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/event_processor.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/event_processor.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/event_processor.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/event_processor.rs	2026-02-01 02:38:16.820436103 -0500
@@ -4,11 +4,11 @@
 use std::slice;
 use std::sync::{Arc, Mutex};
 
-use x11_dl::xinput2::{
+use x11::xinput2::{
     self, XIDeviceEvent, XIEnterEvent, XIFocusInEvent, XIFocusOutEvent, XIHierarchyEvent,
     XILeaveEvent, XIModifierState, XIRawEvent,
 };
-use x11_dl::xlib::{
+use x11::xlib::{
     self, Display as XDisplay, Window as XWindow, XAnyEvent, XClientMessageEvent, XConfigureEvent,
     XDestroyWindowEvent, XEvent, XExposeEvent, XKeyEvent, XMapEvent, XPropertyEvent,
     XReparentEvent, XSelectionEvent, XVisibilityEvent, XkbAnyEvent, XkbStateRec,
@@ -17,7 +17,7 @@
 use x11rb::protocol::xkb::ID as XkbId;
 use x11rb::protocol::xproto::{self, ConnectionExt as _, ModMask};
 use x11rb::x11_utils::{ExtensionInformation, Serialize};
-use xkbcommon_dl::xkb_mod_mask_t;
+use crate::platform_impl::common::xkb::xkb_mod_mask_t;
 
 use crate::dpi::{PhysicalPosition, PhysicalSize};
 use crate::event::{
@@ -134,9 +134,8 @@
     /// arrow keys from being detected twice.
     #[must_use]
     fn filter_event(&mut self, xev: &mut XEvent) -> bool {
-        let wt = Self::window_target(&self.target);
         unsafe {
-            (wt.xconn.xlib.XFilterEvent)(xev, {
+            xlib::XFilterEvent(xev, {
                 let xev: &XAnyEvent = xev.as_ref();
                 xev.window
             }) == xlib::True
@@ -320,7 +319,7 @@
 
     pub fn poll(&self) -> bool {
         let window_target = Self::window_target(&self.target);
-        let result = unsafe { (window_target.xconn.xlib.XPending)(window_target.xconn.display) };
+        let result = unsafe { xlib::XPending(window_target.xconn.display) };
 
         result != 0
     }
@@ -343,7 +342,7 @@
         }
 
         let result = unsafe {
-            (window_target.xconn.xlib.XCheckIfEvent)(
+            xlib::XCheckIfEvent(
                 window_target.xconn.display,
                 event_ptr,
                 Some(predicate),
@@ -1627,23 +1626,58 @@
     ) where
         F: FnMut(&RootAEL, Event<T>),
     {
+        // XInput2 reports modifier/group state as signed integers.
+        //
+        // On some systems (notably 32-bit / i586 builds) we've observed invalid negative values
+        // reaching here. Casting those values to `u32` produces huge numbers, which can trigger
+        // crashes inside libxkbcommon when we call `xkb_state_update_mask`.
+        //
+        // Be defensive: if any values are invalid, fall back to querying XKB state from X11.
+        let window_id = match self.active_window.map(super::mkwid) {
+            Some(window_id) => window_id,
+            None => return,
+        };
+
+        let Some(mods_depressed) = u32::try_from(mods.base).ok() else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
+        let Some(mods_latched) = u32::try_from(mods.latched).ok() else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
+        let Some(mods_locked) = u32::try_from(mods.locked).ok() else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
+        let Some(depressed_group) = u32::try_from(group.base).ok().filter(|g| *g <= 64) else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
+        let Some(latched_group) = u32::try_from(group.latched).ok().filter(|g| *g <= 64) else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
+        let Some(locked_group) = u32::try_from(group.locked).ok().filter(|g| *g <= 64) else {
+            self.update_mods_from_query(window_id, callback);
+            return;
+        };
+
         if let Some(state) = self.xkb_context.state_mut() {
             state.update_modifiers(
-                mods.base as u32,
-                mods.latched as u32,
-                mods.locked as u32,
-                group.base as u32,
-                group.latched as u32,
-                group.locked as u32,
+                mods_depressed,
+                mods_latched,
+                mods_locked,
+                depressed_group,
+                latched_group,
+                locked_group,
             );
 
-            // NOTE: we use active window since generally sub windows don't have keyboard input,
-            // and winit assumes that unfocused window doesn't have modifiers.
-            let window_id = match self.active_window.map(super::mkwid) {
-                Some(window_id) => window_id,
-                None => return,
-            };
-
             let mods = state.modifiers();
             self.send_modifiers(window_id, mods.into(), force, &mut callback);
         }
@@ -1665,7 +1699,7 @@
 
         unsafe {
             let mut state: XkbStateRec = std::mem::zeroed();
-            if (wt.xconn.xlib.XkbGetState)(wt.xconn.display, XkbId::USE_CORE_KBD.into(), &mut state)
+            if xlib::XkbGetState(wt.xconn.display, XkbId::USE_CORE_KBD.into(), &mut state)
                 == xlib::True
             {
                 xkb_state.update_modifiers(
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ffi.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ffi.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ffi.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ffi.rs	2026-01-25 13:37:39.967292673 -0500
@@ -1,5 +1,4 @@
-pub use x11_dl::error::OpenError;
-pub use x11_dl::xcursor::*;
-pub use x11_dl::xinput2::*;
-pub use x11_dl::xlib::*;
-pub use x11_dl::xlib_xcb::*;
+pub use x11::xcursor::*;
+pub use x11::xinput2::*;
+pub use x11::xlib::*;
+pub use x11::xlib_xcb::*;
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/callbacks.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/callbacks.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/callbacks.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/callbacks.rs	2026-02-01 02:38:16.820436103 -0500
@@ -17,7 +17,7 @@
 ) -> Result<(), XError> {
     // It's advisable to wrap variadic FFI functions in our own functions, as we want to minimize
     // access that isn't type-checked.
-    unsafe { (xconn.xlib.XSetIMValues)(xim, field, callback, ptr::null_mut::<()>()) };
+    unsafe { ffi::XSetIMValues(xim, field, callback, ptr::null_mut::<()>()) };
     xconn.check_errors()
 }
 
@@ -32,7 +32,7 @@
     client_data: ffi::XPointer,
 ) -> Result<(), XError> {
     unsafe {
-        (xconn.xlib.XRegisterIMInstantiateCallback)(
+        ffi::XRegisterIMInstantiateCallback(
             xconn.display,
             ptr::null_mut(),
             ptr::null_mut(),
@@ -49,7 +49,7 @@
     client_data: ffi::XPointer,
 ) -> Result<(), XError> {
     unsafe {
-        (xconn.xlib.XUnregisterIMInstantiateCallback)(
+        ffi::XUnregisterIMInstantiateCallback(
             xconn.display,
             ptr::null_mut(),
             ptr::null_mut(),
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/context.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/context.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/context.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/context.rs	2026-02-01 02:38:16.820436103 -0500
@@ -3,7 +3,7 @@
 use std::sync::Arc;
 use std::{mem, ptr};
 
-use x11_dl::xlib::{XIMCallback, XIMPreeditCaretCallbackStruct, XIMPreeditDrawCallbackStruct};
+use super::ffi::{XIMCallback, XIMPreeditCaretCallbackStruct, XIMPreeditDrawCallbackStruct};
 
 use super::{ffi, util, XConnection, XError};
 use crate::platform_impl::platform::x11::ime::input_method::{InputMethod, Style, XIMStyle};
@@ -220,9 +220,7 @@
             Style::Nothing(style) => unsafe {
                 ImeContext::create_nothing_ic(xconn, im.im, style, window)
             },
-            Style::None(style) => unsafe {
-                ImeContext::create_none_ic(xconn, im.im, style, window)
-            },
+            Style::None(style) => unsafe { ImeContext::create_none_ic(xconn, im.im, style, window) },
         }
         .ok_or(ImeContextCreationError::Null)?;
 
@@ -244,13 +242,13 @@
     }
 
     unsafe fn create_none_ic(
-        xconn: &Arc<XConnection>,
+        _xconn: &Arc<XConnection>,
         im: ffi::XIM,
         style: XIMStyle,
         window: ffi::Window,
     ) -> Option<ffi::XIC> {
         let ic = unsafe {
-            (xconn.xlib.XCreateIC)(
+            ffi::XCreateIC(
                 im,
                 ffi::XNInputStyle_0.as_ptr() as *const _,
                 style,
@@ -272,7 +270,7 @@
     ) -> Option<ffi::XIC> {
         let preedit_callbacks = PreeditCallbacks::new(client_data);
         let preedit_attr = util::memory::XSmartPointer::new(xconn, unsafe {
-            (xconn.xlib.XVaCreateNestedList)(
+            ffi::XVaCreateNestedList(
                 0,
                 ffi::XNPreeditStartCallback_0.as_ptr() as *const _,
                 &(preedit_callbacks.start_callback) as *const _,
@@ -288,7 +286,7 @@
         .expect("XVaCreateNestedList returned NULL");
 
         let ic = unsafe {
-            (xconn.xlib.XCreateIC)(
+            ffi::XCreateIC(
                 im,
                 ffi::XNInputStyle_0.as_ptr() as *const _,
                 style,
@@ -304,13 +302,13 @@
     }
 
     unsafe fn create_nothing_ic(
-        xconn: &Arc<XConnection>,
+        _xconn: &Arc<XConnection>,
         im: ffi::XIM,
         style: XIMStyle,
         window: ffi::Window,
     ) -> Option<ffi::XIC> {
         let ic = unsafe {
-            (xconn.xlib.XCreateIC)(
+            ffi::XCreateIC(
                 im,
                 ffi::XNInputStyle_0.as_ptr() as *const _,
                 style,
@@ -325,14 +323,14 @@
 
     pub(crate) fn focus(&self, xconn: &Arc<XConnection>) -> Result<(), XError> {
         unsafe {
-            (xconn.xlib.XSetICFocus)(self.ic);
+            ffi::XSetICFocus(self.ic);
         }
         xconn.check_errors()
     }
 
     pub(crate) fn unfocus(&self, xconn: &Arc<XConnection>) -> Result<(), XError> {
         unsafe {
-            (xconn.xlib.XUnsetICFocus)(self.ic);
+            ffi::XUnsetICFocus(self.ic);
         }
         xconn.check_errors()
     }
@@ -356,7 +354,7 @@
         unsafe {
             let preedit_attr = util::memory::XSmartPointer::new(
                 xconn,
-                (xconn.xlib.XVaCreateNestedList)(
+                ffi::XVaCreateNestedList(
                     0,
                     ffi::XNSpotLocation_0.as_ptr(),
                     &self.ic_spot,
@@ -365,7 +363,7 @@
             )
             .expect("XVaCreateNestedList returned NULL");
 
-            (xconn.xlib.XSetICValues)(
+            ffi::XSetICValues(
                 self.ic,
                 ffi::XNPreeditAttributes_0.as_ptr() as *const _,
                 preedit_attr.ptr,
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/inner.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/inner.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/inner.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/inner.rs	2026-02-01 02:38:16.820436103 -0500
@@ -9,12 +9,12 @@
 use crate::platform_impl::platform::x11::ime::ImeEventSender;
 
 pub(crate) unsafe fn close_im(xconn: &Arc<XConnection>, im: ffi::XIM) -> Result<(), XError> {
-    unsafe { (xconn.xlib.XCloseIM)(im) };
+    unsafe { ffi::XCloseIM(im) };
     xconn.check_errors()
 }
 
 pub(crate) unsafe fn destroy_ic(xconn: &Arc<XConnection>, ic: ffi::XIC) -> Result<(), XError> {
-    unsafe { (xconn.xlib.XDestroyIC)(ic) };
+    unsafe { ffi::XDestroyIC(ic) };
     xconn.check_errors()
 }
 
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/input_method.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/input_method.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/ime/input_method.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/ime/input_method.rs	2026-02-01 02:38:16.820436103 -0500
@@ -17,11 +17,9 @@
     // * The new locale modifiers if we succeeded in setting them.
     // * NULL if the locale modifiers string is malformed or if the current locale is not supported
     //   by Xlib.
-    unsafe { (xconn.xlib.XSetLocaleModifiers)(locale_modifiers.as_ptr()) };
+    unsafe { ffi::XSetLocaleModifiers(locale_modifiers.as_ptr()) };
 
-    let im = unsafe {
-        (xconn.xlib.XOpenIM)(xconn.display, ptr::null_mut(), ptr::null_mut(), ptr::null_mut())
-    };
+    let im = unsafe { ffi::XOpenIM(xconn.display, ptr::null_mut(), ptr::null_mut(), ptr::null_mut()) };
 
     if im.is_null() {
         None
@@ -39,12 +37,12 @@
 }
 
 impl InputMethod {
-    fn new(xconn: &Arc<XConnection>, im: ffi::XIM, name: String) -> Option<Self> {
+    fn new(_xconn: &Arc<XConnection>, im: ffi::XIM, name: String) -> Option<Self> {
         let mut styles: *mut XIMStyles = std::ptr::null_mut();
 
         // Query the styles supported by the XIM.
         unsafe {
-            if !(xconn.xlib.XGetIMValues)(
+            if !ffi::XGetIMValues(
                 im,
                 ffi::XNQueryInputStyle_0.as_ptr() as *const _,
                 (&mut styles) as *mut _,
@@ -73,7 +71,7 @@
                     _ => (),
                 });
 
-            (xconn.xlib.XFree)(styles.cast());
+            ffi::XFree(styles.cast());
         };
 
         if preedit_style.is_none() && none_style.is_none() {
@@ -166,7 +164,7 @@
     let atoms = xconn.atoms();
     let servers_atom = atoms[XIM_SERVERS];
 
-    let root = unsafe { (xconn.xlib.XDefaultRootWindow)(xconn.display) };
+    let root = unsafe { ffi::XDefaultRootWindow(xconn.display) };
 
     let mut atoms: Vec<ffi::Atom> = xconn
         .get_property::<xproto::Atom>(
@@ -181,7 +179,7 @@
 
     let mut names: Vec<*const c_char> = Vec::with_capacity(atoms.len());
     unsafe {
-        (xconn.xlib.XGetAtomNames)(
+        ffi::XGetAtomNames(
             xconn.display,
             atoms.as_mut_ptr(),
             atoms.len() as _,
@@ -196,7 +194,7 @@
             .to_owned()
             .into_string()
             .map_err(GetXimServersError::InvalidUtf8)?;
-        unsafe { (xconn.xlib.XFree)(name as _) };
+        unsafe { ffi::XFree(name as _) };
         formatted_names.push(string.replace("@server=", "@im="));
     }
     xconn.check_errors().map_err(GetXimServersError::XError)?;
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -190,12 +190,12 @@
             // Remember default locale to restore it if target locale is unsupported
             // by Xlib
             let default_locale = setlocale(LC_CTYPE, ptr::null());
-            setlocale(LC_CTYPE, b"\0".as_ptr() as *const _);
+            setlocale(LC_CTYPE, c"".as_ptr() as *const _);
 
             // Check if set locale is supported by Xlib.
             // If not, calls to some Xlib functions like `XSetLocaleModifiers`
             // will fail.
-            let locale_supported = (xconn.xlib.XSupportsLocale)() == 1;
+            let locale_supported = ffi::XSupportsLocale() == 1;
             if !locale_supported {
                 let unsupported_locale = setlocale(LC_CTYPE, ptr::null());
                 warn!(
@@ -731,6 +731,7 @@
 }
 
 struct DeviceInfo<'a> {
+    #[allow(dead_code)]
     xconn: &'a XConnection,
     info: *const ffi::XIDeviceInfo,
     count: usize,
@@ -740,7 +741,7 @@
     fn get(xconn: &'a XConnection, device: c_int) -> Option<Self> {
         unsafe {
             let mut count = 0;
-            let info = (xconn.xinput2.XIQueryDevice)(xconn.display, device, &mut count);
+            let info = ffi::XIQueryDevice(xconn.display, device, &mut count);
             xconn.check_errors().ok()?;
 
             if info.is_null() || count == 0 {
@@ -755,7 +756,7 @@
 impl Drop for DeviceInfo<'_> {
     fn drop(&mut self) {
         assert!(!self.info.is_null());
-        unsafe { (self.xconn.xinput2.XIFreeDeviceInfo)(self.info as *mut _) };
+        unsafe { ffi::XIFreeDeviceInfo(self.info as *mut _) };
     }
 }
 
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/cookie.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/cookie.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/cookie.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/cookie.rs	2026-02-01 02:38:16.820436103 -0500
@@ -1,7 +1,7 @@
 use std::ffi::c_int;
 use std::sync::Arc;
 
-use x11_dl::xlib::{self, XEvent, XGenericEventCookie};
+use super::super::ffi::{self, XEvent, XGenericEventCookie};
 
 use crate::platform_impl::x11::XConnection;
 
@@ -17,7 +17,7 @@
     pub fn from_event(xconn: Arc<XConnection>, event: XEvent) -> Option<GenericEventCookie> {
         unsafe {
             let mut cookie: XGenericEventCookie = From::from(event);
-            if (xconn.xlib.XGetEventData)(xconn.display, &mut cookie) == xlib::True {
+            if ffi::XGetEventData(xconn.display, &mut cookie) == ffi::True {
                 Some(GenericEventCookie { cookie, xconn })
             } else {
                 None
@@ -49,7 +49,7 @@
 impl Drop for GenericEventCookie {
     fn drop(&mut self) {
         unsafe {
-            (self.xconn.xlib.XFreeEventData)(self.xconn.display, &mut self.cookie);
+            ffi::XFreeEventData(self.xconn.display, &mut self.cookie);
         }
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/cursor.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/cursor.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/cursor.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/cursor.rs	2026-02-01 02:38:16.820436103 -0500
@@ -30,9 +30,9 @@
     fn create_empty_cursor(&self) -> ffi::Cursor {
         let data = 0;
         let pixmap = unsafe {
-            let screen = (self.xlib.XDefaultScreen)(self.display);
-            let window = (self.xlib.XRootWindow)(self.display, screen);
-            (self.xlib.XCreateBitmapFromData)(self.display, window, &data, 1, 1)
+            let screen = ffi::XDefaultScreen(self.display);
+            let window = ffi::XRootWindow(self.display, screen);
+            ffi::XCreateBitmapFromData(self.display, window, &data, 1, 1)
         };
 
         if pixmap == 0 {
@@ -43,7 +43,7 @@
             // We don't care about this color, since it only fills bytes
             // in the pixmap which are not 0 in the mask.
             let mut dummy_color = MaybeUninit::uninit();
-            let cursor = (self.xlib.XCreatePixmapCursor)(
+            let cursor = ffi::XCreatePixmapCursor(
                 self.display,
                 pixmap,
                 pixmap,
@@ -52,7 +52,7 @@
                 0,
                 0,
             );
-            (self.xlib.XFreePixmap)(self.display, pixmap);
+            ffi::XFreePixmap(self.display, pixmap);
 
             cursor
         }
@@ -67,12 +67,8 @@
         let mut xcursor = 0;
         for &name in iter::once(&cursor.name()).chain(cursor.alt_names().iter()) {
             let name = CString::new(name).unwrap();
-            xcursor = unsafe {
-                (self.xcursor.XcursorLibraryLoadCursor)(
-                    self.display,
-                    name.as_ptr() as *const c_char,
-                )
-            };
+            xcursor =
+                unsafe { ffi::XcursorLibraryLoadCursor(self.display, name.as_ptr() as *const c_char) };
 
             if xcursor != 0 {
                 break;
@@ -126,7 +122,7 @@
         cursor: PlatformCustomCursorSource,
     ) -> CustomCursor {
         unsafe {
-            let ximage = (event_loop.xconn.xcursor.XcursorImageCreate)(
+            let ximage = ffi::XcursorImageCreate(
                 cursor.0.width as i32,
                 cursor.0.height as i32,
             );
@@ -145,9 +141,8 @@
                     | (chunk[3] as u32) << 24;
             }
 
-            let cursor =
-                (event_loop.xconn.xcursor.XcursorImageLoadCursor)(event_loop.xconn.display, ximage);
-            (event_loop.xconn.xcursor.XcursorImageDestroy)(ximage);
+            let cursor = ffi::XcursorImageLoadCursor(event_loop.xconn.display, ximage);
+            ffi::XcursorImageDestroy(ximage);
             Self { inner: Arc::new(CustomCursorInner { xconn: event_loop.xconn.clone(), cursor }) }
         }
     }
@@ -162,7 +157,7 @@
 impl Drop for CustomCursorInner {
     fn drop(&mut self) {
         unsafe {
-            (self.xconn.xlib.XFreeCursor)(self.xconn.display, self.cursor);
+            ffi::XFreeCursor(self.xconn.display, self.cursor);
         }
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/input.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/input.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/input.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/input.rs	2026-02-01 02:38:16.820436103 -0500
@@ -33,8 +33,7 @@
         mask: xkb::EventType,
     ) -> Result<bool, X11Error> {
         let mask = u16::from(mask) as _;
-        let status =
-            unsafe { (self.xlib.XkbSelectEvents)(self.display, device_id as _, mask, mask) };
+        let status = unsafe { ffi::XkbSelectEvents(self.display, device_id as _, mask, mask) };
 
         if status == ffi::True {
             self.flush_requests()?;
@@ -66,7 +65,7 @@
         let mut keysym: ffi::KeySym = 0;
         let mut status: ffi::Status = 0;
         let count = unsafe {
-            (self.xlib.Xutf8LookupString)(
+            ffi::Xutf8LookupString(
                 ic,
                 key_event,
                 buffer as *mut c_char,
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/keys.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/keys.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/keys.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/keys.rs	2026-02-01 02:38:16.820436103 -0500
@@ -63,7 +63,7 @@
         let mut keys = [0; 32];
 
         unsafe {
-            (self.xlib.XQueryKeymap)(self.display, keys.as_mut_ptr() as *mut c_char);
+            ffi::XQueryKeymap(self.display, keys.as_mut_ptr() as *mut c_char);
         }
 
         Keymap { keys }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/memory.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/memory.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/memory.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/memory.rs	2026-01-25 23:07:54.036261659 -0500
@@ -3,6 +3,7 @@
 use super::*;
 
 pub(crate) struct XSmartPointer<'a, T> {
+    #[allow(dead_code)]
     xconn: &'a XConnection,
     pub ptr: *mut T,
 }
@@ -36,7 +37,7 @@
 impl<T> Drop for XSmartPointer<'_, T> {
     fn drop(&mut self) {
         unsafe {
-            (self.xconn.xlib.XFree)(self.ptr as *mut _);
+            ffi::XFree(self.ptr as *mut _);
         }
     }
 }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/modifiers.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/modifiers.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/modifiers.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/modifiers.rs	2026-01-25 13:40:42.706263734 -0500
@@ -57,7 +57,7 @@
 
             self.reset_from_x_keymap(&*keymap);
 
-            (xconn.xlib.XFreeModifiermap)(keymap);
+            ffi::XFreeModifiermap(keymap);
         }
     }
 
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/mod.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/mod.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/mod.rs	2026-02-01 02:38:16.820436103 -0500
@@ -62,7 +62,7 @@
     //    and waits.
     // All util functions that abstract an async function will return a `Flusher`.
     pub fn flush_requests(&self) -> Result<(), XError> {
-        unsafe { (self.xlib.XFlush)(self.display) };
+        unsafe { ffi::XFlush(self.display) };
         // println!("XFlush");
         // This isn't necessarily a useful time to check for errors (since our request hasn't
         // necessarily been processed yet)
@@ -70,7 +70,7 @@
     }
 
     pub fn sync_with_server(&self) -> Result<(), XError> {
-        unsafe { (self.xlib.XSync)(self.display, ffi::False) };
+        unsafe { ffi::XSync(self.display, ffi::False) };
         // println!("XSync");
         self.check_errors()
     }
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/xmodmap.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/xmodmap.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/util/xmodmap.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/util/xmodmap.rs	2026-02-01 02:38:16.824436168 -0500
@@ -1,7 +1,7 @@
 use std::collections::HashSet;
 use std::slice;
 
-use x11_dl::xlib::{KeyCode as XKeyCode, XModifierKeymap};
+use super::super::ffi::{self, KeyCode as XKeyCode, XModifierKeymap};
 
 // Offsets within XModifierKeymap to each set of keycodes.
 // We are only interested in Shift, Control, Alt, and Logo.
@@ -30,7 +30,7 @@
 
     pub fn reload_from_x_connection(&mut self, xconn: &super::XConnection) {
         unsafe {
-            let keymap = (xconn.xlib.XGetModifierMapping)(xconn.display);
+            let keymap = ffi::XGetModifierMapping(xconn.display);
 
             if keymap.is_null() {
                 return;
@@ -38,7 +38,7 @@
 
             self.reset_from_x_keymap(&*keymap);
 
-            (xconn.xlib.XFreeModifiermap)(keymap);
+            ffi::XFreeModifiermap(keymap);
         }
     }
 
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/window.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/window.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/window.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/window.rs	2026-02-01 02:38:16.824436168 -0500
@@ -517,7 +517,7 @@
             // Attempt to make keyboard input repeat detectable
             unsafe {
                 let mut supported_ptr = ffi::False;
-                (xconn.xlib.XkbSetDetectableAutoRepeat)(
+                ffi::XkbSetDetectableAutoRepeat(
                     xconn.display,
                     ffi::True,
                     &mut supported_ptr,
diff -uraN /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/xdisplay.rs /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/xdisplay.rs
--- /home/a/ctoolbox/vendor/winit.orig/src/platform_impl/linux/x11/xdisplay.rs	2006-07-23 21:21:28.000000000 -0400
+++ /home/a/ctoolbox/vendor/winit/src/platform_impl/linux/x11/xdisplay.rs	2026-02-01 02:38:16.824436168 -0500
@@ -17,13 +17,6 @@
 
 /// A connection to an X server.
 pub struct XConnection {
-    pub xlib: ffi::Xlib,
-    pub xcursor: ffi::Xcursor,
-
-    // TODO(notgull): I'd like to remove this, but apparently Xlib and Xinput2 are tied together
-    // for some reason.
-    pub xinput2: ffi::XInput2,
-
     pub display: *mut ffi::Display,
 
     /// The manager for the XCB connection.
@@ -67,18 +60,12 @@
 
 impl XConnection {
     pub fn new(error_handler: XErrorHandler) -> Result<XConnection, XNotSupported> {
-        // opening the libraries
-        let xlib = ffi::Xlib::open()?;
-        let xcursor = ffi::Xcursor::open()?;
-        let xlib_xcb = ffi::Xlib_xcb::open()?;
-        let xinput2 = ffi::XInput2::open()?;
-
-        unsafe { (xlib.XInitThreads)() };
-        unsafe { (xlib.XSetErrorHandler)(error_handler) };
+        unsafe { ffi::XInitThreads() };
+        unsafe { ffi::XSetErrorHandler(error_handler) };
 
         // calling XOpenDisplay
         let display = unsafe {
-            let display = (xlib.XOpenDisplay)(ptr::null());
+            let display = ffi::XOpenDisplay(ptr::null());
             if display.is_null() {
                 return Err(XNotSupported::XOpenDisplayFailed);
             }
@@ -88,8 +75,7 @@
         // Open the x11rb XCB connection.
         let xcb = {
             // Get a pointer to the underlying XCB connection
-            let xcb_connection =
-                unsafe { (xlib_xcb.XGetXCBConnection)(display as *mut ffi::Display) };
+            let xcb_connection = unsafe { ffi::XGetXCBConnection(display as *mut ffi::Display) };
             assert!(!xcb_connection.is_null());
 
             // Wrap the XCB connection in an x11rb XCB connection
@@ -100,7 +86,7 @@
         };
 
         // Get the default screen.
-        let default_screen = unsafe { (xlib.XDefaultScreen)(display) } as usize;
+        let default_screen = unsafe { ffi::XDefaultScreen(display) } as usize;
 
         // Load the database.
         let database = resource_manager::new_from_default(&xcb)
@@ -125,9 +111,6 @@
             .map_err(|e| XNotSupported::XcbConversionError(Arc::new(e)))?;
 
         Ok(XConnection {
-            xlib,
-            xcursor,
-            xinput2,
             display,
             xcb: Some(xcb),
             atoms: Box::new(atoms),
@@ -269,7 +252,7 @@
     #[inline]
     fn drop(&mut self) {
         self.xcb = None;
-        unsafe { (self.xlib.XCloseDisplay)(self.display) };
+        unsafe { ffi::XCloseDisplay(self.display) };
     }
 }
 
@@ -297,9 +280,6 @@
 /// Error returned if this system doesn't have XLib or can't create an X connection.
 #[derive(Clone, Debug)]
 pub enum XNotSupported {
-    /// Failed to load one or several shared libraries.
-    LibraryOpenError(ffi::OpenError),
-
     /// Connecting to the X server with `XOpenDisplay` failed.
     XOpenDisplayFailed, // TODO: add better message.
 
@@ -307,17 +287,9 @@
     XcbConversionError(Arc<dyn Error + Send + Sync + 'static>),
 }
 
-impl From<ffi::OpenError> for XNotSupported {
-    #[inline]
-    fn from(err: ffi::OpenError) -> XNotSupported {
-        XNotSupported::LibraryOpenError(err)
-    }
-}
-
 impl XNotSupported {
     fn description(&self) -> &'static str {
         match self {
-            XNotSupported::LibraryOpenError(_) => "Failed to load one of xlib's shared libraries",
             XNotSupported::XOpenDisplayFailed => "Failed to open connection to X server",
             XNotSupported::XcbConversionError(_) => "Failed to convert Xlib connection to XCB",
         }
@@ -328,7 +300,6 @@
     #[inline]
     fn source(&self) -> Option<&(dyn Error + 'static)> {
         match *self {
-            XNotSupported::LibraryOpenError(ref err) => Some(err),
             XNotSupported::XcbConversionError(ref err) => Some(&**err),
             _ => None,
         }
